# Pitch-Investor Matching

A stub Laravel project containing models and a service class for matching pitches with investors based on their preferences.

## Structure

- `app/Models/` – `Pitch`, `Investor`, `InvestorPreference`
- `app/Services/` – `InvestorMatchingService`
- `tests/Unit/` – `InvestorMatchingTest`
